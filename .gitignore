# Rust build artifacts
**/target/

# Local env/secrets
.env
.env.*
!.env.example

# Local runtime state (Railway volume path is /data in prod, but local defaults may create ./data)
data/
**/*.sqlite
**/*.sqlite-wal
**/*.sqlite-shm

# Local Codex clone (reference-only; do not commit into template repo)
codex/

# Reference repos cloned during development (do not commit)
nanobot/
coworker/
guardrails/
frontend/node_modules/*
**/*.tsbuildinfo
